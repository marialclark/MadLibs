import MadLib from './components/MadLib';

/**
 * The App componet is the root of the application.
 * It renders the MadLib component, which contains all the app logic
 * and structure.
 */
const App = () => {
	return (
		<div>
			<MadLib />
		</div>
	);
};

export default App;
